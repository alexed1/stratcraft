<aura:component>
    <ltng:require scripts="{!$Resource.utils}" />
    
    <aura:registerEvent name="buttonClickEvent" type="c:modalWindowButtonClickEvent" />

    <lightning:button name="cancel" label="Cancel" onclick="{!c.handleCancel}" />
    <lightning:button name="ok" label="Continue" variant="brand" onclick="{!c.handleOK}" />
</aura:component>